
Insert and Get operations of a User (Id, Name, Email, Status)

When you insert a record, it should be logged into a file log info (
    (date and time, action("insert/get"))

the log should be a goroutine and also a channel

It should be a file based insert. id should be random numbers


for true{

1. Insert
    Enter Name
    Enter Email

--> Successfully saved

2. Get
    Enter Email: 
    Fetch the data searching in a file 
}

