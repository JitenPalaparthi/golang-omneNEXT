### Go run 

```bash
go run .
```

### Create a mod

```bash
go mod init demo
```

glpat-65yucg8YlE2jjbOjVsb_e286MQp1OmhqbWEyCw.01.120gvradm


### Keywords

break,case,chan,contiune,const,default,defer, else, fallthrough, for, func, go,goto, if,import,interface, map,package,range, return,select, switch, struct, type, var 


### Keycloak

```bash
 podman run  --name keycloak -d -p 127.0.0.1:18080:8080 -e KC_BOOTSTRAP_ADMIN_USERNAME=admin -e KC_BOOTSTRAP_ADMIN_PASSWORD=admin quay.io/keycloak/keycloak:latest start-dev
 ```
